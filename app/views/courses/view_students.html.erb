<h1> student roster page </h1>
<% @students.each do |student| %>
    <p> name: <%= @student.name %> </p>
    <%= link_to "show_student", student_path(student) %>

<% end %>
<br> <%= link_to "Back to course info", course_path(@course) %>