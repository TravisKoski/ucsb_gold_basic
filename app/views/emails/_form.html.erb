<%= form_with(model: email) do |form| %>
  <% if email.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(email.errors.count, "error") %> prohibited this email from being saved:</h2>

      <ul>
        <% email.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :sender, style: "display: block" %>
    <%= form.text_field :sender %>
  </div>

  <div>
    <%= form.label :reciever, style: "display: block" %>
    <%= form.text_field :reciever %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.hidden_field :Student_id, style: "display: block" %>
    <%= form.hidden_field :Student_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
